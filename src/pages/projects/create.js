import React from 'react';
import ProjectContainer from 'src/components/containers/project';

const CreateProjectPage = ({ props }) => {
  return <ProjectContainer />;
};

export default CreateProjectPage;
